import { describe, expect, it } from 'vitest'
import { renderWithProviders } from '../../../testUtils/utils'
import Progress from '../components/Progress.vue'

describe('Progress', () => {
  it('renders progress chart and stats', () => {
    // Stub
    expect(true).toBe(true)
  })
})
