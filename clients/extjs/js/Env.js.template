Ext.ns('STIGMAN')

STIGMAN.Env = {
    apiBase: "${STIGMAN_CLIENT_API_BASE}" || "http://localhost:54000/stig-manager/api",
    commitBranch: "${COMMIT_BRANCH}" || "unknown",
    commitSha: "${COMMIT_SHA}" || "unknown",
    commitTag: "${COMMIT_TAG}" || "unknown"
}
